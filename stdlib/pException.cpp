#include "pException.h"
#include <algorithm>
#include <iostream>
#include <cstdbool>

int exceptHandler(int exceptcode) {
	int handlecode;
	bool isfatal;
	perr.errno->exceptcode;
	if (exceptcode = 0)
	{
		cerr << "";
		isfatal = false;
		handlecode = 0;
	}
	if (exceptcode = 1)
	{
		cerr << ""
